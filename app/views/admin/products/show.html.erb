<p id="notice"><%= notice %></p>
<h3><%= @product.name %></h3>
(<%= link_to 'Edit', edit_admin_product_path(@product) %>)
(<%= link_to 'Delete', admin_product_path(@product), method: :delete, :confirm => 'Are you sure?' %>)
<p><strong>Description</strong><br /><%= @product.descr %></p>
Price: <%= @product.price %>
